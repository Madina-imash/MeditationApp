//
//  MainView.swift
//  MeditationApp
//
//  Created by Smart Castle M1A2002 on 28.03.2025.
//

/*import SwiftUI

//struct MainView: View {
    var body: some View {
        NavigationView {
            VStack {
                Text("Выберите медитацию")
                    .font(.largeTitle)
                    .padding()

                // Категории медитаций
                VStack {
                    Button("Расслабление") { }
                    Button("Фокус и концентрация") { }
                    Button("Засыпание") { }
                    Button("Энергия") { }
                }
                .buttonStyle(.bordered)
                .padding()

                // Кнопка выбора настроения
                NavigationLink(destination: MoodSelectionView()) {
                    Text("Выбрать по настроению")
                        .font(.headline)
                        .padding()
                        .frame(maxWidth: .infinity)
                        .background(Color.blue)
                        .foregroundColor(.white)
                        .cornerRadius(10)
                }
                .padding()
            }
        }
    }
}

#Preview {
    //MainView()
 }*/
