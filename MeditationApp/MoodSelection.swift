//
//  MoodSelection.swift
//  MeditationApp
//
//  Created by Smart Castle M1A2002 on 28.03.2025.
//

/*import SwiftUI

struct MoodSelectionView: View {
    @State private var selectedMood: String = "Спокойствие"
    let moods = ["Спокойствие", "Расслабление", "Фокус", "Энергия"]
    
    var body: some View {
        VStack {
            Text("Выберите настроение")
                .font(.largeTitle)
                .padding()
            
            Picker("Выберите настроение", selection: $selectedMood) {
                ForEach(moods, id: \.self) { mood in
                    Text(mood)
                }
            }
            .pickerStyle(MenuPickerStyle())
            .padding()
            
            //Text("Вы выбрали:
            
        }
    }
 }*/
